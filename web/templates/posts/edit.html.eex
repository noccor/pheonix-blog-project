<h2>Edit posts</h2>

<%= render "form.html", changeset: @changeset,
                        action: posts_path(@conn, :update, @posts) %>

<span><%= link "Back", to: posts_path(@conn, :index) %></span>
